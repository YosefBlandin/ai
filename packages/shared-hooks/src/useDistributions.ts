/**
 * @fileoverview Hook factory for creating useDistributions hook
 * Re-exports the hook factory for backward compatibility
 */

// Re-export the hook factory for backward compatibility
export { createUseDistributionsHook } from '@aidonic/shared-utils';
