/**
 * @fileoverview Hook factory for creating useDistribution hook
 * Re-exports the hook factory for backward compatibility
 */

// Re-export the hook factory for backward compatibility
export { createUseDistributionHook } from '@aidonic/shared-utils';
