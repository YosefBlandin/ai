/**
 * @fileoverview Distributions page
 */

import React from 'react';
import { DistributionListContainer } from '@/features/distributions';

export default function DistributionsPage() {
  return <DistributionListContainer />;
}
